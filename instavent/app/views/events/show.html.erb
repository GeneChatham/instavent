<h1>#<%= @event.tag %></h1>
<h3><%= @event.start_time %></h3>
<h3><%= @event.end_time %></h3>

<% @event.sort_photos.each do |photo| %>
    <img src="<%= photo.image %>" class="event-photos">
    <!-- Ruby Script to show time stamps -->
    <!-- Time.at(photo.time).strftime("%m/%d/%Y at %I:%M%p") -->


<% end %>
